$(document).ready(() => {
    // Blicking index page
    // window.setInterval(() => {
    //     $('.blick').toggleClass('invisible');
    // }, 400);


    $(document.querySelector('input')).focus();
});