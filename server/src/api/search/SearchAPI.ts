// Search engine:
// 1. search products with the name like the query
// 2. get the keywords (each product has keywords)
// 3. send them back

// Here we always have a string `query` in the req.params
export const SearchAPI = {
    /**
     * Return ALL found products by query
     */
    findByQuery(req, res) {

    },

    /**
     * Return keywords of the first found product
     */
    getTipsByQuery(req, res) {

    }
}